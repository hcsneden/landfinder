{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true
  },
  "comment": "Shared base config for Node/backend workspaces (packages/shared, services/*). Each workspace has its own tsconfig that extends this with the right `include`. The web and mobile apps have standalone configs. Run `npm run typecheck` to check every workspace.",
  "files": [],
  "exclude": ["node_modules", "dist", ".expo", "cdk.out", "build"]
}
